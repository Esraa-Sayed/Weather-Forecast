package com.example.weatherforecast.AlertScreen.view

import com.example.weatherforecast.Model.UserAlerts

interface OnButtonClickListener {
    fun onDeleteButtonClickListener(userAlert: UserAlerts)
}